query PseParams($height: Int) {
  pse_params: action_pse_params {
    params {
      clearing_account_mappings {
        clearing_account
        recipient_addresses
      }
      excluded_addresses
    }
  }
}
